Reflection Questions (attached to PR)
What did you implement?
    I implemented the Banshee class, which is a subclass of Creature. I implemented the banshees special move and I implemented the constructor for the Banshee class, which sets name health and damage 
What does override allow your class to do?
    override allows the Banshee class to provide its own implementation of the specialMove function, which is defined as a virtual function in the Creature class. This means that when we call specialMove on a Creature pointer or reference that actually points to a Banshee object
What are changes we made this week to our code?
    we added virtual functions to attack, added special move, changed arenarand to be rng